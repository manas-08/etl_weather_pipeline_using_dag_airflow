apiVersion: 1

datasources:
  - name: Weather PostgreSQL
    type: postgres
    access: proxy
    url: weather_postgres:5432    # Use internal container port, not external
    database: weather_db
    user: postgres
    secureJsonData:
      password: '20p'
    jsonData:
      sslmode: 'disable'
      maxOpenConns: 100
      maxIdleConns: 100
      maxIdleConnsAuto: true
      connMaxLifetime: 14400
      postgresVersion: 1300
    isDefault: true
    editable: true